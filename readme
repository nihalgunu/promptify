chrome://extensions/

upload extension components

# pip python3 -m venv promptify-env
# source promptify-env/bin/activate
# pip install -r requirements.txt

run the server

should work at that point.